namespace: grafana

bases:
  - ../../base

resources:
  - rbac.yaml

patches:
  - path: deployment.yaml
    target:
      kind: Deployment
      version: v1
      group: apps
      name: grafana-operator-controller-manager
