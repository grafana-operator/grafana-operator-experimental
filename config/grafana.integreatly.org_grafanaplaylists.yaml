
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: grafanaplaylists.grafana.integreatly.org
spec:
  group: grafana.integreatly.org
  names:
    kind: GrafanaPlayList
    listKind: GrafanaPlayListList
    plural: grafanaplaylists
    singular: grafanaplaylist
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: GrafanaPlayList is the Schema for the GrafanaPlayLists API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GrafanaPlayListSpec defines the desired state of GrafanaPlayList
            properties:
              dashboards:
                description: PlaylistDashboards is a list of dashboards that should
                  be in the playlist
                items:
                  properties:
                    order:
                      description: The order of the dashboard in the playlist
                      format: int64
                      type: integer
                    title:
                      description: The title of the dashboard in the playlist
                      type: string
                    type:
                      enum:
                      - dashboard_by_id
                      - dashboard_by_tag
                      type: string
                    value:
                      type: string
                  required:
                  - order
                  - title
                  - type
                  - value
                  type: object
                type: array
              instanceSelector:
                description: selects Grafanas for import
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
              interval:
                description: Interval how often the playlist should change dashboard
                type: string
              name:
                description: Name of the playlist
                type: string
            required:
            - interval
            - name
            type: object
          status:
            description: GrafanaPlayListStatus defines the observed state of GrafanaPlayList
            properties:
              hash:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
